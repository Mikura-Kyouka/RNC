# RusticNest Compiler

## 简介

RNC是一个用 Rust 实现的 SNL 语言编译器。

## 预期目标

### 阶段1
* 词法分析模块（使用 `Logos`）(已实现）
* 语法分析模块（使用 `LALRPOP` ）（已实现）
* 语义分析模块（已实现）

### 阶段2（未确定是否纳入计划）
* 中间代码生成
* 中间代码优化
* 目标代码生成（已实现）

## 计划支持的目标架构
如果进行到**阶段2**，将支持如下指令集架构。
* `mipsel`（如课程要求则首选）
* `loongarch32r`（如无要求则首选）（已实现）
